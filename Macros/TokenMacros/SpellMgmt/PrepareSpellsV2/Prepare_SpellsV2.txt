
	[h:input("spellName|Fireball|Which spell do you want to prepare?","numSpells|1|How many uses?")]
	[h:url =  "https://vxe45sf1th.execute-api.us-east-1.amazonaws.com/1/spells?name=" +spellName]
	[h:response = REST.get(url,0)]
	[if(json.get(response,"statusCode")==404),code:{
				[h:input("spellName|Spell Not Found|Name of Spell","spellDur|1 round / Level|Duration of Spell","spellDesc|This spell slaps|Spell Description","save|Refortill|Saving throw","range|420 ft|Spell Range","spellArea|Bigass circle|Area of Spell","spellLink|https://www.youtube.com/watch?v=V4akMaeZ0-k|Link to spell")]
		};{
	[h:body = json.get(response,"body")]
	[h:allNames = ""]
	[h,foreach(spell,body),code:{
		[h:allNames = allNames + (replace(json.get(spell,"name"),",","") +",")]
		}]
	[h:input("chosen|" + allNames + "|Which spell?|RADIO")]
	[h:chosenSpell = json.get(body,chosen)]
	[h:spellName = json.get(chosenSpell,"name")]
	[h:spellDur = json.get(chosenSpell,"duration")]
	[h:spellDesc = getGroup(strfind(json.get(chosenSpell,"description"),"^(.*?)[.?!]"),1,1)]
	[h:spellDesc = replace(spellDesc,"'","")]
	[h:spellArea = json.get(chosenSpell,"area")]
	[h:spellLink = json.get(chosenSpell,"linktext")]
	[h:range = json.get(chosenSpell,"range")]
	[h:save = json.get(chosenSpell,"saving_throw")]
			}]
	[h:command = encode("[h:input("+'"' + "use|Cast,Recharge|What would you like to do?|RADIO|ORIENT=H" + '"' + ")]")]
	[h:command = command + encode("[h:color = " + '"' + "red" + '"' + "]")]
	[h:command = command + encode("[if(use == 0),code:{[h:spellUsesLeft = json.get(Extra8," + '"' +spellName + '"' + ")]")]
	[h:command = command + encode("	[if(spellUsesLeft > 0),code:{")]
	[h:command = command + encode("Casting " + spellName + "!")]
	[h:command = command + encode("<ul>")]
	[h:command = command + encode("<li>Range: " + range + "</li>")]
	[h:command = command + encode("<li>Duration: " + spellDur + "</li>")]
	[h:command = command + encode("<li>Description: " + spellDesc + "</li>")]
	[h:command = command + encode("<li><a href=" + "'" + spellLink + "'>" + spellName + "</a></li>")]
	[h:command = command + encode("</ul>")]
	[h:command = command + encode("	[h:thisMacro = getMacroProps(getMacroButtonIndex(),"+'"' + "json" + '"' + ")]")]
	[h:command = command + encode("	[h: spellUsesLeft = spellUsesLeft - 1]")]
	[h:command = command + encode("[h:Extra8 = json.set(Extra8," + '"' + spellName +'"' + ",spellUsesLeft)]")]
	[h:command = command + encode("[h:setMacroProps(getMacroButtonIndex()," + '"' + "label=") ]
	[h:command = command + encode(spellName + " - " + '"' +" + spellUsesLeft)]")]
	[h:command = command + encode("};{")]
	[h:command = command + encode("	[h:setMacroProps(getMacroButtonIndex()," + '"' +"label=" +  spellName + " - " + '"' +" + spellUsesLeft)]")]
	[h:command = command + encode("[s:"+'"' + "Out of uses" + '"' + "]")]
	[h:command = command + encode("}]};{")]
	[h:command = command + encode("[h:input(" + '"' + "numSpells|0|How many uses?" + '"' + ")]")]
	[h:command = command + encode("[h:Extra8 = json.set(Extra8," + '"' +spellName + '"' + ",numSpells)]")]
	[h:command = command + encode("[h:setMacroProps(getMacroButtonIndex()," + '"' +"label=" +  spellName + " - " + '"' +" + numSpells)]")]
	[h:command = command + encode("[h:spellUsesLeft = json.get(Extra8," + '"' + spellName + '"' + ")]}]")]
	[h:command = command + encode("[h,if(spellUsesLeft == 0),code:{[h:color = 'red']};{[h:color = 'lime']}]")]
	[h:command = command + encode("[h:setMacroProps(getMacroButtonIndex(),'color=' + color)]")]
	[h:label = spellName + " - " + numSpells]
	[h:macroProps="autoexec=true;playerEditable=1;color=lime"]
	[h:group="Prepared Spells"]
	[h:macroProps=setStrProp(macroProps,"group",group)]
	[h:createMacro(label, decode(command), macroProps)]
	[h:Extra8 = json.set(Extra8,spellName,numSpells)]
